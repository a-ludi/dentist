package:
  version:  "local"

source:
  git_rev: HEAD
  git_url: "{{ environ.get("GIT_ROOT", "../../..") }}"

requirements:
  run:
    - dentist-core==local
    - snakemake==6.10.0
    - pygraphviz==1.7 # NOTE: fixes some bug in the dependency solver
